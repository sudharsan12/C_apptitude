#include<stdio.h>
int main()
{
	float a=0.7;
	if(a<0.7)
		printf("C");
	else
		printf("c++");
	// the result is c

	double a=0.7;
	if(a<0.7)
		printf("C");
	else
		printf("c++");
	// the result is c++
}
